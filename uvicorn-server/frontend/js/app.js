/**
 * APP.JS
 * Инициализация приложения
 * Загружаем модули: render.js, ui.js, api.js перед этим файлом
 */

window.onload = updateDashboard;

// для автообновления каждые 5 секунд:
//setInterval(updateDashboard, 5000);